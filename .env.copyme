# Copy to .env to configure local Make commands (leave empty to use PATH).
# Optional executable directories, separated by colons. Do not add quotes.
# Example: EXTRA_PATH=/path/to/dotnet:/path/to/roslyn-tools
EXTRA_PATH=
